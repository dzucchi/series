import React from "react";
import { StyleSheet, View, Text, FlatList } from "react-native";

const SerieFormPage = props => (
    <View>
        <Text>Serie Form Page</Text>        
    </View>
);

const styles = StyleSheet.create({

});

export default SerieFormPage;